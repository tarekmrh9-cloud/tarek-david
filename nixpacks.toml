[phases.setup]
nixPkgs = [
  "nodejs_20",
  "python3",
  "pkg-config",
  "pixman",
  "cairo",
  "pango",
  "libjpeg",
  "libpng",
  "giflib"
]

[phases.install]
cmds = [
  "npm install --legacy-peer-deps --no-audit --prefer-offline",
  "node scripts/fix-esm.js"
]

[start]
cmd = "node David.js"
